{
  "name": "nowcasting_alerts_cron",
  "version": "0.0.0",
  "devDependencies": {
    "wrangler": "2.1.10"
  },
  "private": true,
  "scripts": {
    "start": "npx miniflare src/index.js --modules --live-reload --kv ALERTS --kv LOG",
    "deploy": "wrangler publish"
  }
}
