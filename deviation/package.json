{
  "name": "deviation",
  "version": "0.0.1",
  "description": "Checks for deivation between OCF and PV_Live forecasts.",
  "main": "index.js",
  "devDependencies": {},
  "scripts": {
    "dev": "npx miniflare index.js --modules --live-reload --kv ALERTS --kv LOG",
    "deploy": "wrangler publish"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/openclimatefix/nowcasting_alerts_cron.git"
  }
}
